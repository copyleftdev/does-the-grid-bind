\* CONTROL: the grid keeps up. EnergizeRate 2 == ShipRate 2.
\* EXPECTED OUTCOME: NoFreeAbsorption IS VIOLATED. TLC must produce a witness
\* schedule that racks every shipped unit with zero impairment. If this run
\* passes, the constrained result is vacuous -- the spec would be forbidding
\* clean absorption for some reason other than the power constraint.
CONSTANTS
    MaxTime = 4
    ShipRate = 2
    EnergizeRate = 2
    InitEnergized = 2
    InitOld = 2
    Life = 6
    Cap = 12
INIT Init
NEXT Next
INVARIANTS
    TypeOK
    I1_Conservation
    I2_Envelope
    I4_BookConservation
    NoFreeAbsorption
