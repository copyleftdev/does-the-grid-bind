\* THE ARTICLE'S WORLD: the vendor ships faster than the grid energizes.
\* ShipRate 2 units/period against EnergizeRate 1 unit/period.
\* EXPECTED OUTCOME: no violation. Every operator strategy that clears the
\* warehouse must write something off.
CONSTANTS
    MaxTime = 4
    ShipRate = 2
    EnergizeRate = 1
    InitEnergized = 2
    InitOld = 2
    Life = 6
    Cap = 12
INIT Init
NEXT Next
INVARIANTS
    TypeOK
    I1_Conservation
    I2_Envelope
    I4_BookConservation
    Probe_NeverAbsorbed
